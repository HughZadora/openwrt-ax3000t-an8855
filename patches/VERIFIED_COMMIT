# OpenWrt main 已验证 commit 基准
#
# 在改动这个文件前,请先在一台 Linux 上:
#   1) git clone --depth 1 --branch main https://git.openwrt.org/openwrt/openwrt.git
#   2) 应用 patches/ 下全部补丁(dry-run 全过、生效校验通过)
#   3) bash setup.sh build 成功,且 check-image-size.sh 报告 initramfs FIT ≤ 26MB
# 然后把该 commit 的完整 sha 写到下面第一行(并更新本文件头注释的验证信息)。
#
# 置空(仅保留注释)则 setup.sh 跟随 main 最新、不锁定,补丁可能漂移失效。
#
# 上次验证:2026-09-18,基线 928cd26(main),本地全新完整构建 bash setup.sh build 成功:
#   产物 initramfs-kernel.bin(FIT)= 20.7MB(≤26MB,体积门 STRICT=1 通过),
#   initramfs-factory.ubi= 22.4MB / squashfs-sysupgrade.bin= 22.2MB /
#   OpenClash apk= 7.8MB(sha256 7d862af45a9c540d…),详见 docs/reports/health-check-2026-09.md
#

928cd26bd938b8ac46b79e14f5f9f4b1d772abe8