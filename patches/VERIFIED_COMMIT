# OpenWrt main verified commit baseline
#
# Before modifying this file, please do the following on a Linux machine first:
#   1) git clone --depth 1 --branch main https://git.openwrt.org/openwrt/openwrt.git
#   2) Apply all patches under patches/ (dry-run all pass, effectiveness check passes)
#   3) bash setup.sh build succeeds, and check-image-size.sh reports initramfs FIT ≤ 26MB
# Then write the full sha to the first line below (and update the verification info in the file header comment).
#
# If blanked (comments only), setup.sh follows main latest without locking; patches may drift.
#
# Last verified: 2026-08-15, output initramfs-kernel.bin(FIT)= 24.9MB(≤26MB),
#   initramfs-factory.ubi=27MB / squashfs-sysupgrade.bin=28MB / OpenClash apk 7.8MB
9f3157aa2ad5481de947bbfbba3e2eb065486a26
